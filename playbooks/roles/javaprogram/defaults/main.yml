---
# defaults file for javaprogram

app_name: javaprogram
image_name: registry:5000/javaprogram
version: latest
root: /opt/javaprogram

directories:
  - "{{root}}/{{ version }}/config"
  - "{{root}}/log"

template_files:
  - application.yml

copy_files:
  - xxx.x